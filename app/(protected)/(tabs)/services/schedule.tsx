import { ThemedView } from "@/components/ThemedView";
import { ThemedText } from "@/components/ThemedText";

export default function ScheduleServiceScreen() {
    return (
        <ThemedView>
            <ThemedText>Schedule Service</ThemedText>
        </ThemedView>
    );
}